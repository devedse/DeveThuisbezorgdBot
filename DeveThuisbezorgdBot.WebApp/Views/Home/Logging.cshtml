@model List<string>

@{
    ViewData["Title"] = "Logging";
}

<div class="row">
    <div class="col-md-6">
        <h2>Logging</h2>
        <ul>
            @foreach (var item in Model)
            {
                <li>@item</li>
            }
        </ul>
    </div>
</div>
